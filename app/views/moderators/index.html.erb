<%= render "navigation", active: :index %>

<h3>trades with negative feedback which need mod approval</h3>

<%= render "trades/trade_list", trades: @mod_pending_trades, pagination_param: :mod_pending_page %>


<h3>trades which haven't been accepted by both parties</h3>

<%= render "trades/trade_list", trades: @not_accepted_trades, pagination_param: :not_accepted_page %>
