hi, we're writing to let you know that a bad trade has been reported for <%= participant.user %>.  please review and manually approve if this is the appropriate action:

<%= edit_trade_participant_url(participant.trade, participant) %>
