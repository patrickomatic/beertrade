hi, wanted to let you know that <%= participant.other_participant.user %> has left <%= participant.feedback_type %> feedback for you:

<%= trade_url(participant.trade, notification_id: notification.id) %>
